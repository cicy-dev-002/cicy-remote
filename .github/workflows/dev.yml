name: Dev

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  dev-ubuntu:
    runs-on: ubuntu-latest
    timeout-minutes: 3600
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Boot
        run: bash .github/workflows/tools/boot.sh

